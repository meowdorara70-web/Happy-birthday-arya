Drop these two files in this folder (optional — the site works fine without them):

1. photo.jpg   -> shows automatically in the "memory" photo scene instead of the placeholder icon.
2. music.mp3   -> plays when the music button (top-right) is tapped.

Nothing else needs to change — script.js already looks for these exact filenames.
